# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'hwahea' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Network
  pod 'Moya', '~> 13.0'
  
  # UI
  pod 'SnapKit', '~> 5.0.0'
  
  # Utility
  pod 'Kingfisher', '~> 4.0'
  # Pods for hwahea

  target 'hwaheaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'hwaheaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
